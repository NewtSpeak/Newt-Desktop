# Newt-Desktop 发布：
# - Windows / Linux / macOS 桌面安装包
# - 可独立部署的 Web 前端包
# - 本仓 Release + 同步到 NewtSpeak/NewtSpeak
#
# 可选 Secrets（代码签名）：
#   TAURI_SIGNING_* / APPLE_*
# 必选（同步中心仓）：
#   RELEASE_HUB_TOKEN
name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      version:
        description: "版本号（不含 v 前缀，如 0.1.0）"
        required: true
        type: string
      extra_notes:
        description: "附加到 Release 说明的额外备注（可选）"
        required: false
        type: string
        default: ""

permissions:
  contents: write

env:
  RELEASE_HUB_REPO: NewtSpeak/NewtSpeak

jobs:
  resolve-version:
    name: Resolve version
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.v.outputs.version }}
      tag: ${{ steps.v.outputs.tag }}
    steps:
      - name: Compute version
        id: v
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="${{ inputs.version }}"
          elif [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            VERSION="${GITHUB_REF_NAME#v}"
          else
            echo "需要 tag 或 version 输入" >&2
            exit 1
          fi
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "tag=v${VERSION}" >> "$GITHUB_OUTPUT"

  # ---------- 独立 Web 前端包 ----------
  pack-web:
    name: Pack standalone web
    needs: resolve-version
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install & build SPA
        run: |
          bun install --frozen-lockfile || bun install
          bun run build
          test -f build/client/index.html

      - name: Pack zip
        env:
          VERSION: ${{ needs.resolve-version.outputs.version }}
        run: |
          chmod +x scripts/pack-web/pack.sh
          sudo apt-get update -qq && sudo apt-get install -y -qq zip
          VERSION="$VERSION" ./scripts/pack-web/pack.sh

      - uses: actions/upload-artifact@v4
        with:
          name: newt-desktop-web-${{ needs.resolve-version.outputs.version }}
          path: |
            dist-web/newt-desktop-web-*.zip
            dist-web/newt-desktop-web-*.sha256
          if-no-files-found: error
          retention-days: 14

  # ---------- 桌面安装包（win / linux / macos） ----------
  build-tauri:
    name: Desktop ${{ matrix.name }}
    needs: resolve-version
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: macos-universal
            platform: macos-latest
            args: --target universal-apple-darwin
            rust_targets: aarch64-apple-darwin,x86_64-apple-darwin
            bundle_glob: src-tauri/target/universal-apple-darwin/release/bundle/**
          - name: linux-x64
            platform: ubuntu-22.04
            args: ""
            rust_targets: ""
            bundle_glob: src-tauri/target/release/bundle/**
          - name: windows-x64
            platform: windows-latest
            args: ""
            rust_targets: ""
            bundle_glob: src-tauri/target/release/bundle/**
    runs-on: ${{ matrix.platform }}
    timeout-minutes: 75
    env:
      # src-tauri/.cargo/config.toml 把 target-dir 重定向到了仓库外（本地开发防 dll 锁）；
      # CI 用环境变量（优先级高于 config.toml）强制回到 src-tauri/target，
      # 否则 bundle_glob 收集不到产物（v0.1.0 首发即因此三平台 upload 全空失败）
      CARGO_TARGET_DIR: ${{ github.workspace }}/src-tauri/target
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install frontend dependencies
        run: bun install --frozen-lockfile || bun install

      - name: Install Linux system dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          # 仅装 ayatana：与 libappindicator3-dev 在新 Ubuntu 上互斥冲突
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            librsvg2-dev \
            patchelf \
            libssl-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust_targets }}

      - uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri
          key: ${{ matrix.name }}

      - name: Sync version into manifests
        env:
          VERSION: ${{ needs.resolve-version.outputs.version }}
        shell: bash
        run: |
          node -e "
            const fs = require('fs');
            const p = JSON.parse(fs.readFileSync('package.json','utf8'));
            p.version = process.env.VERSION;
            fs.writeFileSync('package.json', JSON.stringify(p, null, 2) + '\n');
          "
          node -e "
            const fs = require('fs');
            const p = JSON.parse(fs.readFileSync('src-tauri/tauri.conf.json','utf8'));
            p.version = process.env.VERSION;
            fs.writeFileSync('src-tauri/tauri.conf.json', JSON.stringify(p, null, 2) + '\n');
          "
          if [[ "$(uname -s)" == "Darwin" ]]; then
            sed -i.bak -E "s/^version = \".*\"/version = \"${VERSION}\"/" src-tauri/Cargo.toml
          else
            sed -i.bak -E "s/^version = \".*\"/version = \"${VERSION}\"/" src-tauri/Cargo.toml
          fi
          rm -f src-tauri/Cargo.toml.bak
          echo "version set to ${VERSION}"

      # 只构建，不在此创建 Release（统一由 release job 发布）
      - name: Build Tauri bundles
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          # APPLE_* 签名变量暂不传：secrets 未配置时空值也会触发 tauri 走 codesign
          # 并因导入空证书失败（macOS 产出未签名 .app）；配好证书后再恢复：
          # APPLE_CERTIFICATE / APPLE_CERTIFICATE_PASSWORD / APPLE_SIGNING_IDENTITY
          # / APPLE_ID / APPLE_PASSWORD / APPLE_TEAM_ID
        with:
          args: ${{ matrix.args }}
          uploadUpdaterJson: false

      - name: Collect bundles
        shell: bash
        run: |
          mkdir -p collected
          # macOS 自带 bash 3.2 不支持 globstar，统一用 find 递归收集
          BUNDLE_DIR="${{ matrix.bundle_glob }}"
          BUNDLE_DIR="${BUNDLE_DIR%/\*\*}"
          if [[ -d "$BUNDLE_DIR" ]]; then
            # 仅保留安装包常见类型
            find "$BUNDLE_DIR" -type f \( \
              -name '*.dmg' -o -name '*.app.tar.gz' -o -name '*.msi' -o -name '*.exe' \
              -o -name '*.nsis.zip' -o -name '*.deb' -o -name '*.AppImage' \
              -o -name '*.rpm' -o -name '*.updater*' \) -exec cp -v {} collected/ \;
          fi
          # 若过滤后为空，尽量收集 bundle 目录下所有文件
          if [[ -z "$(ls -A collected 2>/dev/null || true)" && -d "$BUNDLE_DIR" ]]; then
            find "$BUNDLE_DIR" -type f -exec cp -v {} collected/ \;
          fi
          ls -lah collected || true
          # 规范化前缀，便于中心仓识别
          VERSION="${{ needs.resolve-version.outputs.version }}"
          for f in collected/*; do
            [[ -f "$f" ]] || continue
            base="$(basename "$f")"
            if [[ "$base" != newt-desktop-* ]]; then
              mv -v "$f" "collected/newt-desktop-${VERSION}-${{ matrix.name }}-${base}"
            fi
          done
          ls -lah collected

      - uses: actions/upload-artifact@v4
        with:
          name: newt-desktop-${{ matrix.name }}-${{ needs.resolve-version.outputs.version }}
          path: collected/*
          if-no-files-found: error
          retention-days: 14

  release:
    name: Release + Hub sync
    needs: [resolve-version, pack-web, build-tauri]
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/download-artifact@v4
        with:
          path: release-assets
          pattern: newt-desktop-*
          merge-multiple: true

      - name: Prepare checksums & list
        working-directory: release-assets
        run: |
          ls -lahR
          find . -type f ! -name '*.sha256' -print0 | sort -z | xargs -0 sha256sum \
            > "SHA256SUMS-newt-desktop-${{ needs.resolve-version.outputs.version }}.txt"
          cat "SHA256SUMS-newt-desktop-${{ needs.resolve-version.outputs.version }}.txt"

      - name: Write extra notes
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.extra_notes != '' }}
        run: |
          printf '%s\n' "${{ inputs.extra_notes }}" > EXTRA_NOTES.md

      - name: Generate release notes
        env:
          VERSION: ${{ needs.resolve-version.outputs.version }}
          TAG: ${{ needs.resolve-version.outputs.tag }}
          GITHUB_SHA: ${{ github.sha }}
        run: |
          chmod +x scripts/ci/generate-release-notes.sh
          EXTRA=""
          if [ -f EXTRA_NOTES.md ]; then EXTRA=EXTRA_NOTES.md; fi
          {
            scripts/ci/generate-release-notes.sh "Newt-Desktop" "$EXTRA"
            echo
            echo "### 本版本产物"
            echo
            echo "| 类型 | 说明 |"
            echo "|------|------|"
            echo "| 桌面安装包 | Windows / Linux / macOS（Universal） |"
            echo "| \`newt-desktop-web-*.zip\` | **纯前端静态包**，可独立部署到 Nginx / CDN / 任意静态托管 |"
            echo
          } | tee RELEASE_BODY.md

      - name: Flatten assets
        run: |
          mkdir -p flat-assets
          find release-assets -type f -print0 | while IFS= read -r -d '' f; do
            base="$(basename "$f")"
            # 避免重名覆盖：若已存在则加目录前缀
            dest="flat-assets/${base}"
            if [[ -e "$dest" ]]; then
              dest="flat-assets/$(echo "$f" | sed 's#^release-assets/##' | tr '/' '-')"
            fi
            cp -v "$f" "$dest"
          done
          ls -lah flat-assets
          test -n "$(ls -A flat-assets)"

      - name: Create own-repo Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.resolve-version.outputs.tag }}
          name: Newt-Desktop ${{ needs.resolve-version.outputs.tag }}
          body_path: RELEASE_BODY.md
          files: flat-assets/*
          fail_on_unmatched_files: true
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Sync to NewtSpeak/NewtSpeak hub
        env:
          RELEASE_HUB_TOKEN: ${{ secrets.RELEASE_HUB_TOKEN }}
          RELEASE_HUB_REPO: ${{ env.RELEASE_HUB_REPO }}
          TAG: ${{ needs.resolve-version.outputs.tag }}
          COMPONENT_KEY: Newt-Desktop
          COMPONENT_TITLE: Newt-Desktop（桌面客户端 + Web 前端包）
          RELEASE_BODY_PATH: RELEASE_BODY.md
          ASSET_GLOBS: "flat-assets/*"
        run: |
          chmod +x scripts/ci/sync-release-hub.sh
          scripts/ci/sync-release-hub.sh
